const friends = ['Bob', 'Jake', 'Chris']

console.log(friends.indexOf('Bob'))
console.log(friends.indexOf('Jessica'))
// IndexOf retorna -1 quando não encontra o elemento no array

console.log(friends.includes('Bob'))
// Includes também pode ser usado para descobrir se o elemento se encotra no array. Retorna um boolean
